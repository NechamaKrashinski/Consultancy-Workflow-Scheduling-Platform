# קובץ הגדרות לטסטים
NODE_ENV=test
JWT_SECRET=test-secret-key-for-jest-testing-environment-very-secure-32-chars

# הגדרות DB לטסטים (לא בשימוש בטסטי יחידה)
DB_HOST=localhost
DB_USER=test_user
DB_PASSWORD=test_password
DB_NAME=test_database

# השבתת לוגים בטסטים
LOG_LEVEL=error
